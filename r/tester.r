# create a scatter plot
#x<-c(1,3,4,6,8,9,12)
#y<-c(5,8,6,10,9,13,12)
#plot(x,y)

#print(power.t.test(type="one.sample",power=0.8,sd=sqrt(10),delta=2))
# also: power.anova.test and power.prop.test

# in console: > install.packages("tm", dependencies=TRUE)
# > source("tester.r")
# or
# $ R -f tester.r

#install.packages("tm", dependencies=TRUE)
# but would need to set a CRAN mirror


# load packages
#library(tm)
#print(require(XML))